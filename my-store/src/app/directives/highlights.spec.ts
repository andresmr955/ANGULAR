import { Highlights } from './highlights';

describe('Highlights', () => {
  it('should create an instance', () => {
    const directive = new Highlights();
    expect(directive).toBeTruthy();
  });
});
